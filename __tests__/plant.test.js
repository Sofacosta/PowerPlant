import { test } from 'picomatch';
import {PowerPlant} from './../src/powerPlant.js';
describe('PowerPlant'), () => {
  let reusablePlant;

  beforeEach(() => {
    reusablePlant = new PowerPlant();
  });

  test('what it will do', () => {
    expect
  });
}